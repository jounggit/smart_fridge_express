<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>관리자 대시보드 - 스마트 냉장고</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #f5f7fa;
      min-height: 100vh;
    }
    .sidebar {
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: 250px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 20px;
    }
    .sidebar-logo {
      display: flex;
      align-items: center;
      gap: 12px;
      padding-bottom: 30px;
      border-bottom: 1px solid rgba(255,255,255,0.2);
      margin-bottom: 30px;
    }
    .sidebar-logo span {
      font-size: 32px;
    }
    .sidebar-logo h2 {
      font-size: 18px;
    }
    .nav-menu {
      list-style: none;
    }
    .nav-menu li {
      margin-bottom: 8px;
    }
    .nav-menu a {
      display: block;
      padding: 12px 16px;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      transition: background 0.3s;
    }
    .nav-menu a:hover, .nav-menu a.active {
      background: rgba(255,255,255,0.2);
    }
    .sidebar-footer {
      position: absolute;
      bottom: 20px;
      left: 20px;
      right: 20px;
    }
    .admin-info {
      background: rgba(255,255,255,0.1);
      padding: 12px;
      border-radius: 8px;
      margin-bottom: 12px;
    }
    .logout-btn {
      display: block;
      width: 100%;
      padding: 12px;
      background: rgba(255,255,255,0.2);
      color: white;
      text-decoration: none;
      text-align: center;
      border-radius: 8px;
      transition: background 0.3s;
    }
    .logout-btn:hover {
      background: rgba(255,255,255,0.3);
    }
    .main-content {
      margin-left: 250px;
      padding: 30px;
    }
    .page-header {
      margin-bottom: 30px;
    }
    .page-header h1 {
      font-size: 28px;
      color: #333;
    }
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }
    .stat-card {
      background: white;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .stat-card h3 {
      color: #666;
      font-size: 14px;
      margin-bottom: 8px;
    }
    .stat-card .value {
      font-size: 36px;
      font-weight: 700;
      color: #333;
    }
    .stat-card.users .value { color: #667eea; }
    .stat-card.fridges .value { color: #10b981; }
    .stat-card.items .value { color: #f59e0b; }
    .card {
      background: white;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .card h2 {
      font-size: 18px;
      color: #333;
      margin-bottom: 20px;
      padding-bottom: 12px;
      border-bottom: 1px solid #eee;
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      text-align: left;
      padding: 12px;
      border-bottom: 1px solid #eee;
    }
    th {
      font-weight: 600;
      color: #666;
      font-size: 14px;
    }
    td {
      color: #333;
    }
  </style>
</head>
<body>
  <aside class="sidebar">
    <div class="sidebar-logo">
      <span>🧊</span>
      <h2>스마트 냉장고<br><small>관리자</small></h2>
    </div>
    <ul class="nav-menu">
      <li><a href="/admin/dashboard" class="active">📊 대시보드</a></li>
      <li><a href="/admin/users">👥 사용자 관리</a></li>
      <li><a href="/admin/fridges">🧊 냉장고 관리</a></li>
    </ul>
    <div class="sidebar-footer">
      <div class="admin-info">
        👤 <%= admin.name %>
      </div>
      <a href="/admin/logout" class="logout-btn">로그아웃</a>
    </div>
  </aside>

  <main class="main-content">
    <div class="page-header">
      <h1>대시보드</h1>
    </div>

    <div class="stats-grid">
      <div class="stat-card users">
        <h3>총 사용자</h3>
        <div class="value"><%= stats.userCount %></div>
      </div>
      <div class="stat-card fridges">
        <h3>총 냉장고</h3>
        <div class="value"><%= stats.fridgeCount %></div>
      </div>
      <div class="stat-card items">
        <h3>총 식품</h3>
        <div class="value"><%= stats.itemCount %></div>
      </div>
    </div>

    <div class="card">
      <h2>최근 가입 사용자</h2>
      <table>
        <thead>
          <tr>
            <th>이름</th>
            <th>이메일</th>
            <th>가입일</th>
          </tr>
        </thead>
        <tbody>
          <% recentUsers.forEach(user => { %>
            <tr>
              <td><%= user.name %></td>
              <td><%= user.email %></td>
              <td><%= new Date(user.createdAt).toLocaleDateString('ko-KR') %></td>
            </tr>
          <% }) %>
          <% if (recentUsers.length === 0) { %>
            <tr>
              <td colspan="3" style="text-align: center; color: #999;">등록된 사용자가 없습니다.</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>
</body>
</html>
