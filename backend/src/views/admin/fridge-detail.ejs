<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= fridge.name %> - 식품 목록</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #f5f7fa;
      min-height: 100vh;
    }
    .sidebar {
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: 250px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 20px;
    }
    .sidebar-logo {
      display: flex;
      align-items: center;
      gap: 12px;
      padding-bottom: 30px;
      border-bottom: 1px solid rgba(255,255,255,0.2);
      margin-bottom: 30px;
    }
    .sidebar-logo span { font-size: 32px; }
    .sidebar-logo h2 { font-size: 18px; }
    .nav-menu { list-style: none; }
    .nav-menu li { margin-bottom: 8px; }
    .nav-menu a {
      display: block;
      padding: 12px 16px;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      transition: background 0.3s;
    }
    .nav-menu a:hover, .nav-menu a.active {
      background: rgba(255,255,255,0.2);
    }
    .sidebar-footer {
      position: absolute;
      bottom: 20px;
      left: 20px;
      right: 20px;
    }
    .admin-info {
      background: rgba(255,255,255,0.1);
      padding: 12px;
      border-radius: 8px;
      margin-bottom: 12px;
    }
    .logout-btn {
      display: block;
      width: 100%;
      padding: 12px;
      background: rgba(255,255,255,0.2);
      color: white;
      text-decoration: none;
      text-align: center;
      border-radius: 8px;
    }
    .main-content {
      margin-left: 250px;
      padding: 30px;
    }
    .page-header {
      margin-bottom: 30px;
      display: flex;
      align-items: center;
      gap: 16px;
    }
    .page-header h1 {
      font-size: 28px;
      color: #333;
    }
    .back-btn {
      background: #e5e7eb;
      color: #374151;
      padding: 8px 16px;
      border-radius: 6px;
      text-decoration: none;
      font-size: 14px;
    }
    .back-btn:hover {
      background: #d1d5db;
    }
    .fridge-info {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      margin-bottom: 20px;
      display: flex;
      gap: 20px;
      align-items: center;
    }
    .fridge-icon {
      font-size: 48px;
    }
    .fridge-details h2 {
      font-size: 20px;
      color: #333;
      margin-bottom: 4px;
    }
    .fridge-details p {
      color: #666;
      font-size: 14px;
    }
    .card {
      background: white;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .card h3 {
      font-size: 18px;
      margin-bottom: 16px;
      color: #333;
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      text-align: left;
      padding: 12px;
      border-bottom: 1px solid #eee;
    }
    th {
      font-weight: 600;
      color: #666;
      font-size: 14px;
    }
    .badge {
      display: inline-block;
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 12px;
      font-weight: 600;
    }
    .badge-danger {
      background: #fee2e2;
      color: #dc2626;
    }
    .badge-warning {
      background: #fef3c7;
      color: #d97706;
    }
    .badge-success {
      background: #d1fae5;
      color: #059669;
    }
    .category-badge {
      background: #e0e7ff;
      color: #4338ca;
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 12px;
    }
    .item-image {
      width: 50px;
      height: 50px;
      object-fit: cover;
      border-radius: 8px;
      background: #f3f4f6;
    }
    .item-name {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .no-image {
      width: 50px;
      height: 50px;
      background: #f3f4f6;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
    }
  </style>
</head>
<body>
  <aside class="sidebar">
    <div class="sidebar-logo">
      <span>🧊</span>
      <h2>스마트 냉장고<br><small>관리자</small></h2>
    </div>
    <ul class="nav-menu">
      <li><a href="/admin/dashboard">📊 대시보드</a></li>
      <li><a href="/admin/users">👥 사용자 관리</a></li>
      <li><a href="/admin/fridges" class="active">🧊 냉장고 관리</a></li>
    </ul>
    <div class="sidebar-footer">
      <div class="admin-info">👤 <%= admin.name %></div>
      <a href="/admin/logout" class="logout-btn">로그아웃</a>
    </div>
  </aside>

  <main class="main-content">
    <div class="page-header">
      <a href="/admin/fridges" class="back-btn">← 목록으로</a>
      <h1>냉장고 상세</h1>
    </div>

    <div class="fridge-info">
      <div class="fridge-icon"><%= fridge.icon || '🧊' %></div>
      <div class="fridge-details">
        <h2><%= fridge.name %></h2>
        <p>소유자: <%= fridge.userId ? fridge.userId.name : '알 수 없음' %> (<%= fridge.userId ? fridge.userId.email : '-' %>)</p>
        <p>생성일: <%= new Date(fridge.createdAt).toLocaleDateString('ko-KR') %></p>
      </div>
    </div>

    <div class="card">
      <h3>식품 목록 (<%= items.length %>개)</h3>
      <table>
        <thead>
          <tr>
            <th>식품명</th>
            <th>카테고리</th>
            <th>수량</th>
            <th>유통기한</th>
            <th>상태</th>
          </tr>
        </thead>
        <tbody>
          <% items.forEach(item => {
            const today = new Date();
            const expDate = new Date(item.expirationDate);
            const diffDays = Math.ceil((expDate - today) / (1000 * 60 * 60 * 24));
            let statusClass = 'badge-success';
            let statusText = '양호';
            if (diffDays < 0) {
              statusClass = 'badge-danger';
              statusText = '만료됨';
            } else if (diffDays <= 3) {
              statusClass = 'badge-warning';
              statusText = diffDays + '일 남음';
            } else {
              statusText = diffDays + '일 남음';
            }
          %>
            <tr>
              <td>
                <div class="item-name">
                  <% if (item.imageUrl) { %>
                    <img src="<%= item.imageUrl %>" alt="<%= item.name %>" class="item-image">
                  <% } else { %>
                    <div class="no-image">🍽️</div>
                  <% } %>
                  <span><%= item.name %></span>
                </div>
              </td>
              <td><span class="category-badge"><%= item.category %></span></td>
              <td><%= item.quantity %> <%= item.unit %></td>
              <td><%= new Date(item.expirationDate).toLocaleDateString('ko-KR') %></td>
              <td><span class="badge <%= statusClass %>"><%= statusText %></span></td>
            </tr>
          <% }) %>
          <% if (items.length === 0) { %>
            <tr>
              <td colspan="5" style="text-align: center; color: #999;">등록된 식품이 없습니다.</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>
</body>
</html>
