<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>냉장고 관리 - 스마트 냉장고</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #f5f7fa;
      min-height: 100vh;
    }
    .sidebar {
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: 250px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 20px;
    }
    .sidebar-logo {
      display: flex;
      align-items: center;
      gap: 12px;
      padding-bottom: 30px;
      border-bottom: 1px solid rgba(255,255,255,0.2);
      margin-bottom: 30px;
    }
    .sidebar-logo span { font-size: 32px; }
    .sidebar-logo h2 { font-size: 18px; }
    .nav-menu { list-style: none; }
    .nav-menu li { margin-bottom: 8px; }
    .nav-menu a {
      display: block;
      padding: 12px 16px;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      transition: background 0.3s;
    }
    .nav-menu a:hover, .nav-menu a.active {
      background: rgba(255,255,255,0.2);
    }
    .sidebar-footer {
      position: absolute;
      bottom: 20px;
      left: 20px;
      right: 20px;
    }
    .admin-info {
      background: rgba(255,255,255,0.1);
      padding: 12px;
      border-radius: 8px;
      margin-bottom: 12px;
    }
    .logout-btn {
      display: block;
      width: 100%;
      padding: 12px;
      background: rgba(255,255,255,0.2);
      color: white;
      text-decoration: none;
      text-align: center;
      border-radius: 8px;
    }
    .main-content {
      margin-left: 250px;
      padding: 30px;
    }
    .page-header {
      margin-bottom: 30px;
    }
    .page-header h1 {
      font-size: 28px;
      color: #333;
    }
    .card {
      background: white;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      text-align: left;
      padding: 12px;
      border-bottom: 1px solid #eee;
    }
    th {
      font-weight: 600;
      color: #666;
      font-size: 14px;
    }
    .view-btn {
      background: #667eea;
      color: white;
      padding: 6px 12px;
      border-radius: 6px;
      text-decoration: none;
      font-size: 14px;
    }
    .view-btn:hover {
      background: #5a67d8;
    }
  </style>
</head>
<body>
  <aside class="sidebar">
    <div class="sidebar-logo">
      <span>🧊</span>
      <h2>스마트 냉장고<br><small>관리자</small></h2>
    </div>
    <ul class="nav-menu">
      <li><a href="/admin/dashboard">📊 대시보드</a></li>
      <li><a href="/admin/users">👥 사용자 관리</a></li>
      <li><a href="/admin/fridges" class="active">🧊 냉장고 관리</a></li>
    </ul>
    <div class="sidebar-footer">
      <div class="admin-info">👤 <%= admin.name %></div>
      <a href="/admin/logout" class="logout-btn">로그아웃</a>
    </div>
  </aside>

  <main class="main-content">
    <div class="page-header">
      <h1>냉장고 관리</h1>
    </div>

    <div class="card">
      <table>
        <thead>
          <tr>
            <th>냉장고 이름</th>
            <th>소유자</th>
            <th>소유자 이메일</th>
            <th>생성일</th>
            <th>관리</th>
          </tr>
        </thead>
        <tbody>
          <% fridges.forEach(fridge => { %>
            <tr>
              <td><%= fridge.name %></td>
              <td><%= fridge.userId ? fridge.userId.name : '알 수 없음' %></td>
              <td><%= fridge.userId ? fridge.userId.email : '-' %></td>
              <td><%= new Date(fridge.createdAt).toLocaleDateString('ko-KR') %></td>
              <td>
                <a href="/admin/fridges/<%= fridge._id %>" class="view-btn">식품 보기</a>
              </td>
            </tr>
          <% }) %>
          <% if (fridges.length === 0) { %>
            <tr>
              <td colspan="5" style="text-align: center; color: #999;">등록된 냉장고가 없습니다.</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>
</body>
</html>
