# 스마트 냉장고 관리 시스템 - 프로젝트 최종 보고서

**과목명:** 고급웹프로그래밍
**프로젝트명:** Smart Fridge (스마트 냉장고 관리 시스템)
**개발 기간:** 2024학년도 2학기

---

## 목차
1. [기획 및 설계](#1-기획-및-설계-20점)
2. [구현 및 기술력](#2-구현-및-기술력-50점)
3. [시연 및 실행 가이드](#3-시연-및-실행-가이드-20점)
4. [주제의 창의성 및 확장성](#4-주제의-창의성-및-확장성-10점)
5. [AI 활용 명시](#5-ai-활용-명시-및-적절성-10점)

---

## 1. 기획 및 설계 (20점)

### 1.1 프로젝트 목표

가정에서 냉장고 내 식품을 효율적으로 관리하고, 유통기한 만료로 인한 음식물 낭비를 줄이기 위한 **스마트 냉장고 관리 시스템** 개발

### 1.2 주요 기능 정의

| 기능 | 설명 |
|------|------|
| 회원 관리 | 회원가입, 로그인, 로그아웃 (JWT 인증) |
| 냉장고 관리 | 다중 냉장고 생성/수정/삭제, 아이콘 및 색상 커스터마이징 |
| 식품 관리 | 식품 등록/수정/삭제, 카테고리 분류, 이미지 업로드 |
| 유통기한 알림 | 임박/만료 식품 자동 분류 및 알림 |
| 위치 관리 | 드래그 앤 드롭으로 냉장고 내 식품 위치 관리 |
| 관리자 페이지 | 사용자/냉장고/식품 통합 관리 (EJS) |

### 1.3 데이터베이스 스키마 설계

#### User (사용자)
```javascript
{
  name: String,           // 사용자 이름 (필수)
  email: String,          // 이메일 (필수, 고유)
  password: String,       // 비밀번호 (bcrypt 해싱)
  timestamps: true        // 생성일/수정일 자동 기록
}
```

#### Fridge (냉장고)
```javascript
{
  name: String,           // 냉장고 이름 (필수)
  description: String,    // 설명
  icon: String,           // 아이콘 이모지 (기본: '🧊')
  color: String,          // 테마 색상 (기본: '#3b82f6')
  userId: ObjectId,       // 소유자 참조 (필수)
  timestamps: true
}
```

#### Item (식품)
```javascript
{
  name: String,           // 식품명 (필수)
  category: String,       // 카테고리 (9종: 채소, 과일, 육류 등)
  quantity: Number,       // 수량 (최소 1)
  unit: String,           // 단위 (개, kg, L 등)
  expirationDate: Date,   // 유통기한 (필수, 인덱스)
  purchaseDate: Date,     // 구매일
  imageUrl: String,       // 이미지 경로
  memo: String,           // 메모
  position: { x, y },     // 냉장고 내 위치 좌표
  fridgeId: ObjectId,     // 냉장고 참조 (필수)
  userId: ObjectId,       // 소유자 참조 (필수)
  timestamps: true
}
// 복합 인덱스: (expirationDate, userId), (fridgeId, userId)
```

#### Admin (관리자)
```javascript
{
  username: String,       // 관리자 ID (고유)
  password: String,       // 비밀번호 (bcrypt 해싱)
  name: String,           // 관리자명
  timestamps: true
}
```

### 1.4 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                        Client                                │
├─────────────────────────────┬───────────────────────────────┤
│   React SPA (프론트엔드)     │   EJS (관리자 페이지)          │
│   - 회원 기능                │   - 서버 사이드 렌더링         │
│   - 냉장고/식품 관리          │   - 세션 인증                 │
│   - 알림 페이지              │   - 통계 대시보드             │
└─────────────────────────────┴───────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Express.js Server                          │
├─────────────────────────────────────────────────────────────┤
│  Routes         │  Controllers      │  Middleware            │
│  - /api/auth    │  - authController │  - JWT 인증            │
│  - /api/fridges │  - fridgeController│ - 세션 인증 (관리자)   │
│  - /api/items   │  - itemController │  - 에러 핸들링         │
│  - /api/upload  │                   │                        │
│  - /admin       │                   │                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     MongoDB Atlas                            │
│  Collections: users, fridges, items, admins                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 구현 및 기술력 (50점)

### 2.1 기술 스택

| 영역 | 기술 |
|------|------|
| **Backend** | Node.js, Express.js |
| **Database** | MongoDB Atlas, Mongoose ODM |
| **인증** | JWT (사용자), Express-Session (관리자) |
| **Frontend** | React 18, TypeScript, Vite |
| **스타일링** | Tailwind CSS |
| **관리자 페이지** | EJS 템플릿 엔진 |
| **파일 업로드** | Multer |

### 2.2 Express 서버 구축

#### 서버 설정 (backend/src/server.js)
```javascript
// 주요 미들웨어 설정
app.use(cors({ origin: process.env.FRONTEND_URL, credentials: true }));
app.use(express.json());
app.use(express.urlencoded({ extended: true }));
app.use(session({ secret: process.env.SESSION_SECRET, ... }));

// 라우트 등록
app.use('/api/auth', authRoutes);
app.use('/api/fridges', fridgeRoutes);
app.use('/api/items', itemRoutes);
app.use('/api/upload', uploadRoutes);
app.use('/admin', adminRoutes);

// EJS 템플릿 설정
app.set('view engine', 'ejs');
app.set('views', path.join(__dirname, 'views'));
```

### 2.3 CRUD 기능 구현

#### 인증 API (직접 구현)
| 메서드 | 엔드포인트 | 기능 | 구현 파일 |
|--------|-----------|------|----------|
| POST | /api/auth/register | 회원가입 | authController.js |
| POST | /api/auth/login | 로그인 (JWT 발급) | authController.js |
| GET | /api/auth/me | 현재 사용자 조회 | authController.js |

#### 냉장고 API (직접 구현)
| 메서드 | 엔드포인트 | 기능 | 구현 파일 |
|--------|-----------|------|----------|
| GET | /api/fridges | 냉장고 목록 조회 | fridgeController.js |
| POST | /api/fridges | 냉장고 생성 | fridgeController.js |
| GET | /api/fridges/:id | 냉장고 상세 조회 | fridgeController.js |
| PUT | /api/fridges/:id | 냉장고 수정 | fridgeController.js |
| DELETE | /api/fridges/:id | 냉장고 삭제 | fridgeController.js |

#### 식품 API (직접 구현)
| 메서드 | 엔드포인트 | 기능 | 구현 파일 |
|--------|-----------|------|----------|
| GET | /api/items | 식품 목록 조회 | itemController.js |
| POST | /api/items | 식품 추가 | itemController.js |
| GET | /api/items/:id | 식품 상세 조회 | itemController.js |
| PUT | /api/items/:id | 식품 수정 | itemController.js |
| DELETE | /api/items/:id | 식품 삭제 | itemController.js |
| GET | /api/items/expiring | 유통기한 임박/만료 조회 | itemController.js |

### 2.4 데이터베이스 연동

#### MongoDB 연결 (직접 구현)
```javascript
// backend/src/config/database.js
const mongoose = require('mongoose');

const connectDB = async () => {
  const conn = await mongoose.connect(process.env.MONGODB_URI);
  console.log(`MongoDB 연결됨: ${conn.connection.host}`);
};
```

#### 쿼리 최적화
- 복합 인덱스 설정으로 조회 성능 향상
- 유통기한 기준 정렬 쿼리 최적화

### 2.5 JWT 인증 시스템 (직접 구현)

```javascript
// backend/src/utils/jwt.js
const generateToken = (userId) => {
  return jwt.sign({ userId }, process.env.JWT_SECRET, { expiresIn: '7d' });
};

const verifyToken = (token) => {
  return jwt.verify(token, process.env.JWT_SECRET);
};

// backend/src/middleware/auth.js
const auth = async (req, res, next) => {
  const token = req.headers.authorization?.replace('Bearer ', '');
  const decoded = verifyToken(token);
  req.userId = decoded.userId;
  next();
};
```

### 2.6 EJS 관리자 페이지 (직접 구현)

#### 관리자 라우트 구조
```
/admin
├── /login          - 로그인 페이지
├── /logout         - 로그아웃
├── /dashboard      - 대시보드 (통계)
├── /users          - 사용자 관리
├── /fridges        - 냉장고 목록
└── /fridges/:id    - 냉장고 상세 (식품 목록)
```

#### EJS 템플릿 예시 (dashboard.ejs)
```html
<div class="stats-grid">
  <div class="stat-card">
    <h3>총 사용자</h3>
    <p class="stat-number"><%= stats.userCount %></p>
  </div>
  <div class="stat-card">
    <h3>총 냉장고</h3>
    <p class="stat-number"><%= stats.fridgeCount %></p>
  </div>
  <div class="stat-card">
    <h3>총 식품</h3>
    <p class="stat-number"><%= stats.itemCount %></p>
  </div>
</div>
```

### 2.7 에러 핸들링 (직접 구현)

```javascript
// backend/src/middleware/error.js
const errorHandler = (err, req, res, next) => {
  // Mongoose 유효성 검사 에러
  if (err.name === 'ValidationError') {
    return res.status(400).json({ error: message });
  }
  // Mongoose 중복 키 에러
  if (err.code === 11000) {
    return res.status(400).json({ error: '이미 존재하는 값입니다' });
  }
  // JWT 에러
  if (err.name === 'JsonWebTokenError') {
    return res.status(401).json({ error: '유효하지 않은 토큰입니다' });
  }
  // 기본 에러
  res.status(500).json({ error: '서버 에러가 발생했습니다' });
};
```

### 2.8 파일 구조

```
project/
├── backend/
│   ├── src/
│   │   ├── config/
│   │   │   └── database.js          # DB 연결
│   │   ├── controllers/
│   │   │   ├── authController.js    # 인증 로직
│   │   │   ├── fridgeController.js  # 냉장고 로직
│   │   │   └── itemController.js    # 식품 로직
│   │   ├── middleware/
│   │   │   ├── auth.js              # JWT 인증
│   │   │   ├── adminAuth.js         # 관리자 인증
│   │   │   └── error.js             # 에러 처리
│   │   ├── models/
│   │   │   ├── User.js              # 사용자 모델
│   │   │   ├── Fridge.js            # 냉장고 모델
│   │   │   ├── Item.js              # 식품 모델
│   │   │   └── Admin.js             # 관리자 모델
│   │   ├── routes/
│   │   │   ├── auth.js              # 인증 라우트
│   │   │   ├── fridges.js           # 냉장고 라우트
│   │   │   ├── items.js             # 식품 라우트
│   │   │   ├── upload.js            # 업로드 라우트
│   │   │   └── admin.js             # 관리자 라우트
│   │   ├── utils/
│   │   │   └── jwt.js               # JWT 유틸리티
│   │   ├── views/admin/             # EJS 템플릿
│   │   │   ├── login.ejs
│   │   │   ├── dashboard.ejs
│   │   │   ├── users.ejs
│   │   │   ├── fridges.ejs
│   │   │   └── fridge-detail.ejs
│   │   └── server.js                # 서버 진입점
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Layout.tsx           # 레이아웃
│   │   │   └── ProtectedRoute.tsx   # 인증 라우트
│   │   ├── contexts/
│   │   │   └── AuthContext.tsx      # 인증 상태
│   │   ├── lib/
│   │   │   ├── api.ts               # API 클라이언트
│   │   │   └── utils.ts             # 유틸리티
│   │   ├── pages/
│   │   │   ├── LandingPage.tsx
│   │   │   ├── LoginPage.tsx
│   │   │   ├── RegisterPage.tsx
│   │   │   ├── DashboardPage.tsx
│   │   │   ├── FridgeDetailPage.tsx
│   │   │   └── NotificationsPage.tsx
│   │   ├── App.tsx
│   │   └── main.tsx
│   └── package.json
│
└── README.md
```

---

## 3. 시연 및 실행 가이드 (20점)

### 3.1 환경 설정

#### 필수 요구사항
- Node.js 18 이상
- MongoDB Atlas 계정 또는 로컬 MongoDB

#### 환경 변수 설정 (backend/.env)
```
PORT=3001
MONGODB_URI=mongodb+srv://...
JWT_SECRET=your-secret-key
SESSION_SECRET=your-session-secret
FRONTEND_URL=http://localhost:5173
```

### 3.2 실행 방법

```bash
# 1. 저장소 클론
git clone [repository-url]

# 2. 백엔드 실행
cd backend
npm install
npm run dev

# 3. 프론트엔드 실행 (새 터미널)
cd frontend
npm install
npm run dev
```

### 3.3 접속 URL

| 페이지 | URL |
|--------|-----|
| 프론트엔드 | http://localhost:5173 |
| 백엔드 API | http://localhost:3001/api |
| 관리자 페이지 | http://localhost:3001/admin |

### 3.4 관리자 계정 설정

1. 초기 설정: `http://localhost:3001/admin/setup` 접속
2. 로그인: `http://localhost:3001/admin/login`
   - 아이디: `admin`
   - 비밀번호: `admin123`

### 3.5 주요 시연 시나리오

1. **회원가입 및 로그인**
   - 랜딩 페이지 → 회원가입 → 자동 로그인

2. **냉장고 관리**
   - 대시보드 → 냉장고 추가 → 아이콘/색상 선택

3. **식품 관리**
   - 냉장고 클릭 → 3D 냉장고 UI → 식품 추가
   - 드래그 앤 드롭으로 위치 조정
   - 식품 클릭 → 상세 정보 확인

4. **유통기한 알림**
   - 알림 페이지에서 임박/만료 식품 확인
   - 대시보드 상단 알림 확인

5. **관리자 기능**
   - 관리자 로그인 → 대시보드 통계 확인
   - 사용자 목록 조회/삭제
   - 전체 냉장고 및 식품 조회

---

## 4. 주제의 창의성 및 확장성 (10점)

### 4.1 창의적 요소

1. **실감나는 3D 냉장고 UI**
   - 문 열기 애니메이션
   - 냉기 효과 및 내부 조명
   - 실제 냉장고처럼 식품 배치

2. **드래그 앤 드롭 위치 관리**
   - 그리드 기반 스냅 기능
   - 위치 데이터 서버 저장
   - 마우스/터치 모두 지원

3. **스마트 유통기한 관리**
   - 자동 분류 (신선/임박/만료)
   - 색상 코딩으로 시각적 구분
   - 실시간 알림

4. **다중 냉장고 지원**
   - 냉장고별 커스터마이징
   - 가정/사무실 등 분리 관리

### 4.2 확장 가능성

| 기능 | 설명 |
|------|------|
| 푸시 알림 | FCM 연동으로 모바일 알림 |
| 바코드 스캔 | 카메라로 식품 자동 등록 |
| 레시피 추천 | 보유 식품 기반 레시피 제안 |
| 가족 공유 | 냉장고 공유 기능 |
| 쇼핑 리스트 | 부족한 식품 자동 목록화 |

---

## 5. AI 활용 명시 및 적절성 (10점)

### 5.1 AI 활용 범위

본 프로젝트에서 AI(Claude)는 **뷰/디자인 관련 작업에만** 활용되었으며, **핵심 기능은 모두 직접 구현**하였습니다.

### 5.2 직접 구현한 핵심 기능 (AI 미사용)

#### 백엔드 (100% 직접 구현)
- Express 서버 설정 및 라우팅 구조
- MongoDB 스키마 설계 및 인덱스 최적화
- JWT 인증 시스템 (토큰 생성/검증/미들웨어)
- CRUD API 전체 (냉장고, 식품, 사용자)
- 유통기한 조회 로직 및 쿼리 최적화
- 에러 핸들링 미들웨어
- 파일 업로드 처리 (Multer)
- 관리자 세션 인증

#### 프론트엔드 (핵심 로직 직접 구현)
- API 클라이언트 설계 (Axios 인터셉터)
- 인증 컨텍스트 (Context API)
- 라우팅 및 인증 보호
- 드래그 앤 드롭 로직
- 그리드 스냅 알고리즘
- 유통기한 계산 유틸리티

#### 관리자 페이지 (100% 직접 구현)
- EJS 템플릿 구조 설계
- 세션 기반 인증 로직
- 서버 사이드 데이터 렌더링
- 관리자 라우트 및 컨트롤러

### 5.3 AI 활용 부분 (뷰/디자인)

| 항목 | 내용 |
|------|------|
| CSS 스타일링 | Tailwind CSS 클래스 조합 |
| 3D 냉장고 UI | 냉기 효과, 조명 애니메이션 CSS |
| 반응형 레이아웃 | 모바일/데스크톱 레이아웃 |
| 색상 조합 | 테마 색상 및 상태 색상 |
| 아이콘 선정 | 카테고리별 이모지 매핑 |

### 5.4 AI 활용 적절성 판단

| 구분 | 직접 구현 | AI 활용 |
|------|----------|---------|
| 서버 구축 | O | - |
| DB 설계 | O | - |
| API 개발 | O | - |
| 인증 시스템 | O | - |
| 비즈니스 로직 | O | - |
| EJS 템플릿 로직 | O | - |
| UI 컴포넌트 구조 | O | - |
| CSS 스타일링 | - | O |
| 애니메이션 효과 | - | O |

**결론:** 핵심 기능(서버, DB, API, 인증, 비즈니스 로직)은 **100% 직접 구현**하였고, AI는 시각적 완성도를 높이기 위한 **디자인 보조 도구**로만 활용하였습니다.

---

## 부록: 핵심 코드 직접 구현 증빙

### A. JWT 인증 (직접 구현)
```javascript
// backend/src/utils/jwt.js
const jwt = require('jsonwebtoken');

exports.generateToken = (userId) => {
  return jwt.sign({ userId }, process.env.JWT_SECRET, { expiresIn: '7d' });
};

exports.verifyToken = (token) => {
  return jwt.verify(token, process.env.JWT_SECRET);
};
```

### B. 유통기한 조회 API (직접 구현)
```javascript
// backend/src/controllers/itemController.js
exports.getExpiringItems = async (req, res, next) => {
  const days = parseInt(req.query.days) || 7;
  const now = new Date();
  const futureDate = new Date();
  futureDate.setDate(futureDate.getDate() + days);

  const expiringItems = await Item.find({
    userId: req.userId,
    expirationDate: { $gte: now, $lte: futureDate },
  }).sort({ expirationDate: 1 });

  const expiredItems = await Item.find({
    userId: req.userId,
    expirationDate: { $lt: now },
  }).sort({ expirationDate: -1 });

  res.json({ expiringItems, expiredItems });
};
```

### C. 드래그 앤 드롭 + 그리드 스냅 (직접 구현)
```typescript
// frontend/src/pages/FridgeDetailPage.tsx
function snapToGrid(x: number, y: number): { x: number; y: number } {
  const col = Math.round((x - PADDING) / CELL_WIDTH);
  const row = Math.round((y - PADDING) / CELL_HEIGHT);

  const boundedCol = Math.max(0, Math.min(col, GRID_COLS - 1));
  const boundedRow = Math.max(0, Math.min(row, GRID_ROWS - 1));

  const snappedX = PADDING + boundedCol * CELL_WIDTH + (CELL_WIDTH - ITEM_WIDTH) / 2;
  const snappedY = PADDING + boundedRow * CELL_HEIGHT + (CELL_HEIGHT - ITEM_HEIGHT) / 2;

  return { x: Math.round(snappedX), y: Math.round(snappedY) };
}
```

---

**작성일:** 2024년
**작성자:** [학번/이름]
